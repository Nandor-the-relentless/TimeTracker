{
  "name": "@app/mobile",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cap run ios --external || cap run android --external",
    "sync": "cap sync",
    "build": "pnpm --filter @app/web build && cap copy"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}